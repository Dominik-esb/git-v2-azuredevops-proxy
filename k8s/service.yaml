apiVersion: v1
kind: Service
metadata:
  name: git-proxy
  namespace: git-proxy
  labels:
    app: git-proxy
spec:
  type: ClusterIP
  selector:
    app: git-proxy
  ports:
    - port: 80
      targetPort: 80
      name: http
    - port: 443
      targetPort: 443
      name: https
